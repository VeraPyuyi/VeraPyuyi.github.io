---
import { getLocaleStaticPaths } from '../_shared/utils';
import BlogsPage from '../blogs.astro';

export const getStaticPaths = getLocaleStaticPaths;

interface Props {
  locale: string;
}

const { locale } = Astro.props;
---

<BlogsPage locale={locale} />
